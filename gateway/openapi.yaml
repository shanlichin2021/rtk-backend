swagger: "2.0"

# ← Global CORS for every response (200, 401, 404, etc.)
x-google-cors:
  allowOrigins: ["https://shanlichin2021.github.io"]
  allowMethods: ["OPTIONS", "POST"]
  allowHeaders: ["Content-Type", "Authorization"]

info:
  title: Right‑To‑Know API
  version: "1.0.0"

paths:
  /api/chat:
    options:
      operationId: chatPreflight
      x-google-allow: all
      responses:
        "204":
          description: "No Content"
    post:
      operationId: chat
      security:
        - id_token: []
      responses:
        "200":
          description: "OK"

x-google-backend:
  address: https://rtk-backend-1054015385247.us-central1.run.app
  protocol: h2

securityDefinitions:
  id_token:
    type: "oauth2"
    flow: "implicit"
    x-google-issuer: "https://accounts.google.com"
    x-google-jwks_uri: "https://www.googleapis.com/oauth2/v3/certs"
    x-google-audiences: https://rtk-backend-1054015385247.us-central1.run.app
